# CloudCore Environment Variables

# ======================================
# AWS S3 Configuration (existing)
# ======================================
# These are set by the user at runtime in the UI
# No need to set here - just for reference

# ======================================
# URL Shortener Configuration (NEW)
# ======================================

# Cloudflare Worker URL for URL shortening service
# Set this to your deployed worker URL (e.g., https://go.cloudcore.app)
# Leave empty to disable URL shortening feature
VITE_SHORTENER_URL=

# API Key for authenticating with the URL shortener worker
# Must match the API_KEY secret set in your Cloudflare Worker
# Generate with: openssl rand -base64 32
VITE_SHORTENER_API_KEY=

# ======================================
# Development Settings
# ======================================
# For local development, you can use these values:
# VITE_SHORTENER_URL=http://localhost:8787
# VITE_SHORTENER_API_KEY=dev-key-replace-in-production
