# CloudCore Environment Variables

# ======================================
# AWS S3 Configuration
# ======================================
# AWS credentials are set by the user at runtime through the UI
# No environment variables are required for the application to run
# 
# The app uses direct S3 access with user-provided credentials
# stored securely in encrypted sessionStorage
